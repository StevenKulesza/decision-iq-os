import { css } from "emotion"
import * as theme from '../theme'

export const navbarFooter = css`
  z-index: 100;
  background-color: ${theme.lightBlue2};
  padding: 20px 0;

  & h5 {
    margin: 5px 0 0;
  }
`
