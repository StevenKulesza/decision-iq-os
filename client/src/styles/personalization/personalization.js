import { css } from 'emotion';
import * as theme from '../theme';

export const personalizationPanel = css `
    background: ${theme.white};
`;

export const templatesPanel = css `
    background: ${theme.lightGray};
`;